apiVersion: v1
kind: Service
metadata:
  name: k8s-interview-demo
  labels:
    app: k8s-interview-demo
spec:
  selector:
    app: k8s-interview-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP