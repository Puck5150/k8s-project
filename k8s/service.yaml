apiVersion: v1
kind: Service
metadata:
  name: k8s-project
  labels:
    app: k8s-project
spec:
  selector:
    app: k8s-project
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP