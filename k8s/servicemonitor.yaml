apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: k8s-project
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: k8s-project
  endpoints:
    - port: http
      path: /metrics
      interval: 15s